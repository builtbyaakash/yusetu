# Dashboard

React admin UI for Yusetu.

```bash
# from repo root
pnpm install
pnpm --filter @yusetu/dashboard dev
```

Dev server: http://127.0.0.1:5173 (proxies `/api` and `/mcp` to gateway on :8080).
